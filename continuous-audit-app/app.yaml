command:
  - sh
  - -c
  - "uvicorn main:app --host 0.0.0.0 --port ${DATABRICKS_APP_PORT:-8000}"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    value: "ed5c06d0f073810c"
  # Ambiente de dados — troque para produção quando migrar:
  #   CA_CATALOG: "compliance"   CA_SCHEMA: "continuous_audit"
  - name: CA_CATALOG
    value: "sandbox"
  - name: CA_SCHEMA
    value: "grc"

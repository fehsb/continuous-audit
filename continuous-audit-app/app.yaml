command:
  - sh
  - -c
  - "uvicorn main:app --host 0.0.0.0 --port ${DATABRICKS_APP_PORT:-8000}"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    value: "ed5c06d0f073810c"
  # Ambiente de dados — PRODUÇÃO (cutover 2026-07-19; rollback: sandbox / grc)
  - name: CA_CATALOG
    value: "compliance"
  - name: CA_SCHEMA
    value: "continuous_audit"
